# Copy this to ".env" and fill in with your real keys (locally only).
PORT=4000

# BaseScan / Etherscan API V2 key (for chainid=8453)
ETHERSCAN_API_KEY=YOUR_BASESCAN_KEY_HERE

# OpenAI key (optional – if missing, app will use fallback persona + story)
OPENAI_API_KEY=YOUR_OPENAI_API_KEY_HERE
